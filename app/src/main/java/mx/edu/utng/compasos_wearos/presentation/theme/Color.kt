package mx.edu.utng.compasos_wearos.presentation.theme

import androidx.compose.ui.graphics.Color

// Primarios — botón SOS / acción crítica
val RojoEmergencia    = Color(0xFF1565C0)  // azul profundo (botón SOS de la imagen)
val RojoOscuro        = Color(0xFF0D47A1)  // variante oscura del botón SOS

// Secundarios — vinculación, acciones
val AzulSeguro        = Color(0xFF1E3A5F)  // azul medio oscuro (superficie activa)
val AzulClaro         = Color(0xFF29B6F6)  // cian eléctrico — acento "SOS" del logo, íconos

// Fondos
val Negro             = Color(0xFF0A1628)  // fondo principal (azul marino muy oscuro)
val GrisOscuro        = Color(0xFF0D1F3C)  // superficie de cards
val GrisMedio         = Color(0xFF162B4D)  // elementos secundarios

// Estado
val VerdeConectado    = Color(0xFF43A047)  // "Activo" — igual que la imagen
val AmarilloAlerta    = Color(0xFFFDD835)  // advertencia
val GrisDesactivado   = Color(0xFF37506B)  // elementos inactivos

// Texto
val Blanco            = Color(0xFFFFFFFF)
val BlancoOpaco       = Color(0xB3FFFFFF)  // texto secundario